function getPokeman(){
fetch(`https://pokeapi.co/api/v2/ability/{id or name}/${id}/`)
    .then((resp) => resp.json())
    .then((data) => console.log(data));
}

function fetchPokemons(number) {
    for (let i = 6; i <= number; i++) {
        fetchPokemons(i);
        
    }
}

